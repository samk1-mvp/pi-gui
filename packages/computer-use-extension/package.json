{
  "name": "@pi-gui/computer-use-extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "displayName": "Computer Use",
  "description": "Control Mac apps from pi",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:locked-failure": "pnpm run build && node scripts/test-locked-failure.mjs"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@types/node": "^22.13.0"
  }
}
