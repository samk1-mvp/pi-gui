{
  "name": "pi-gui",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "dev": "pnpm --filter @pi-gui/desktop dev",
    "bun:dev": "cd apps/desktop && bun run dev",
    "build": "pnpm -r --if-present build",
    "bun:build": "for dir in apps/* packages/*; do (cd \"$dir\" && (grep -q '\"bun:build\":' package.json && bun run bun:build || (grep -q '\"build\":' package.json && bun run build))); done",
    "typecheck": "pnpm --filter @pi-gui/session-driver build && pnpm --filter @pi-gui/catalogs build && pnpm --filter @pi-gui/pi-sdk-driver build && pnpm -r --if-present typecheck",
    "bun:typecheck": "cd packages/session-driver && bun run build && cd ../pi-sdk-driver && bun run build && cd ../catalogs && bun run build && for dir in apps/* packages/*; do (cd \"$dir\" && grep -q '\"typecheck\":' package.json && bun run typecheck); done",
    "bun:package": "cd apps/desktop && bun run bun:package",
    "bun:package:dir": "cd apps/desktop && bun run bun:package:dir",
    "bun:package:linux": "cd apps/desktop && bun run bun:package:linux",
    "bun:package:linux:dir": "cd apps/desktop && bun run bun:package:linux:dir",
    "package:win": "pnpm --filter @pi-gui/desktop run package:win",
    "package:win:dir": "pnpm --filter @pi-gui/desktop run package:win:dir",
    "bun:package:win": "cd apps/desktop && bun run bun:package:win",
    "bun:package:win:dir": "cd apps/desktop && bun run bun:package:win:dir",
    "lint": "pnpm -r --if-present lint",
    "bun:lint": "for dir in apps/* packages/*; do (cd \"$dir\" && (grep -q '\"bun:lint\":' package.json && bun run bun:lint || (grep -q '\"lint\":' package.json && bun run lint))); done",
    "test": "pnpm -r --if-present test",
    "bun:test": "for dir in apps/* packages/*; do (cd \"$dir\" && (grep -q '\"bun:test\":' package.json && bun run bun:test || (grep -q '\"test\":' package.json && bun run test))); done",
    "e2e": "playwright test",
    "test:homebrew-tap": "node scripts/test-update-homebrew-tap.mjs",
    "verify:install-copy": "node scripts/verify-install-copy.mjs",
    "verify:homebrew-flow": "node scripts/verify-homebrew-flow.mjs",
    "release:homebrew-sync": "node scripts/release-homebrew-sync.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^24.0.0",
    "pnpm": "10.25.0",
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "video"
  ]
}